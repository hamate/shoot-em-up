/* eslint-disable lines-between-class-members */
export default class Character {
  hp: number;
  dp: number;
  sp: number;

  constructor(hp: number, dp: number, sp: number) {
    this.hp = hp;
    this.dp = dp;
    this.sp = sp;
  }
}
